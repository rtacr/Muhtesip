import 'package:flutter/material.dart';

Color gridIconColor = Colors.white;
Color bottomNavigation = primaryColor;
Color mainBGColor = Colors.black;
Color primaryColor = Colors.teal;
Color iconColors = primaryColor;
Color niceColor = Colors.blueGrey[600];

IconData globe = Icons.public;

ThemeData anaTema = ThemeData(
  fontFamily: 'Segoe UI',
  primaryColor: primaryColor
);
